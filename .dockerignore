# Secrets — env vars are set in the host's dashboard, never baked into an image.
# An image layer is permanent and readable by anyone who can pull the image.
.env
auth.json

node_modules
.git
.github

# Scrape output and logs
downloads/
debug/
*.log

# Local-only
dashboard/
docs/
*.md
